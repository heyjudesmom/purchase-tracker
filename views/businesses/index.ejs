<%- include('../partials/header') %>

<main id="index">
    <% businesses.forEach(function(b){ %>
        <div class="post">
            <h1> <%= b.name %> </h1>
            <a href="/businesses/<%=b._id%>"> LOCATION </a>
        </div>
        <% }) %>
</main>
<%- include('../partials/footer') %>